import { MilisecToDatePipe } from './milisec-to-date.pipe';

describe('MilisecToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new MilisecToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
